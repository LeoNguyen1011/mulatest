import 'package:flutter/cupertino.dart';

class AppDimensions {
  static const double defaultHorizontalPadding = 16;
  static const double defaultBottomPadding = 52;
  static const EdgeInsets defaultPagePadding = EdgeInsets.only(
    top: 0,
    bottom: defaultBottomPadding,
    left: defaultHorizontalPadding,
    right: defaultHorizontalPadding,
  );
}
